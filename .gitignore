node_modules/
.DS_Store
telemetry-id
# Ignore external reference projects
antigravity-claude-proxy-main/
# Ignore credentials
accounts.json
auth.json
.codex-claude-proxy/
.internal/
# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
pentagi
apps/portfolio/dist/
# Bundled speech-to-text model — fetched at build time via
# `npm run fetch:stt-model` (electron:build runs it), not committed.
public/models/
# Electron build output
dist-electron/
.test-config/
.test-server-*.out.log
.test-server-*.err.log
tests/reports/
.tmp/
